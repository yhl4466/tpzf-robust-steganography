================================================================================================
verify-mobile-scenarios：手机场景验收（合成载体模拟真机，口径与 embed.html 一致）
================================================================================================

── 场景 1：4000×3000 手机照片载体 + 1080×2400 手机截图秘密图 ──
   载体 4000×3000：纹理块 2613，安全块 0，可承载上限约 51.0 KB（分析 1150 ms）
   秘密图原始 1080×2400
   ✅ 成功：实际缩放至 432×960（倍率 0.40），JPEG 质量 0.55，灰度=false，载荷 49.8 KB，K=1276 N=2552，耗时 9094 ms
   提取校验：成功，载荷 51011 字节与嵌入一致；零遮蔽存活 2552/2552 = 100.0%

── 场景 2：4000×3000 手机照片载体 + 4000×3000 手机照片秘密图 ──
   载体 4000×3000：纹理块 2613，安全块 0，可承载上限约 51.0 KB（分析 909 ms）
   秘密图原始 4000×3000
   ✅ 成功：实际缩放至 800×600（倍率 0.20），JPEG 质量 0.55，灰度=false，载荷 49.0 KB，K=1256 N=2512，耗时 24554 ms
   提取校验：成功，载荷 50217 字节与嵌入一致；零遮蔽存活 2512/2512 = 100.0%

── 场景 3a：4000×3000 手机照片载体 + 生成的 1024² 图当秘密图 ──
   载体 4000×3000：纹理块 2613，安全块 0，可承载上限约 51.0 KB（分析 823 ms）
   秘密图原始 1024×1024
   ✅ 成功：实际缩放至 717×717（倍率 0.70），JPEG 质量 0.55，灰度=false，载荷 46.4 KB，K=1188 N=2376，耗时 6696 ms
   提取校验：成功，载荷 47488 字节与嵌入一致；零遮蔽存活 2376/2376 = 100.0%

── 场景 3b（对照）：生成的 2048² 载体 + 场景 2 的秘密图 ──
   载体 2048×2048：纹理块 1024，安全块 1024，可承载上限约 20.0 KB（分析 288 ms）
   秘密图原始 4000×3000
   ✅ 成功：实际缩放至 480×360（倍率 0.12），JPEG 质量 0.45，灰度=false，载荷 19.0 KB，K=488 N=976，耗时 14981 ms
   提取校验：成功，载荷 19472 字节与嵌入一致；零遮蔽存活 976/976 = 100.0%

── 灰度模式收益（同一张秘密图、同一质量 0.75）──
[PASS] AC8 灰度模式生效：同一质量下体积明显变小（本编码器 ≤85%，真实浏览器实测 66%~93%）
        源图彩色度（通道最大差均值）101.8/255；体积比 q0.45=75.2%（1.33×），q0.75=70.7%（1.41×），q0.92=56.8%（1.76×）；真实浏览器（Chromium 内核）实测区间：平滑饱和色 68~71%、高细节彩色 66~74%、照片类 81~93%

── 自适应质量在不同容量下的实际 q ──
[PASS] AC6 自适应压缩：容量富余时质量升到 0.85+，容量紧张时降到 0.65 以下
        容量充足（3000×2000 纹理载体 + 160×120 秘密图）: q=0.92，缩至 160×120；容量紧张（3000×2000 载体 + 3000×2000 秘密图）: q=0.55，缩至 600×400；容量紧张 + 灰度模式（同上，灰度）: q=0.65，缩至 600×400；判定：充足场景 q=0.92（要求 ≥0.85），紧张场景 q=0.55（要求 ≤0.65）

================================================================================================
场景结果：4/4 个场景成功嵌入并完整提取
  ✅ 场景 1：4000×3000 手机照片载体 + 1080×2400 手机截图秘密图 → 缩放 40%、q=0.55、432×960、提取 成功
  ✅ 场景 2：4000×3000 手机照片载体 + 4000×3000 手机照片秘密图 → 缩放 20%、q=0.55、800×600、提取 成功
  ✅ 场景 3a：4000×3000 手机照片载体 + 生成的 1024² 图当秘密图 → 缩放 70%、q=0.55、717×717、提取 成功
  ✅ 场景 3b（对照）：生成的 2048² 载体 + 场景 2 的秘密图 → 缩放 12%、q=0.45、480×360、提取 成功
附加检查失败数：0
================================================================================================
